This app is aim to sovle the problem that django can not expire some key manaully.I will be add some other function related with memcache in django later.

step1: copy the app from this project ;

step2: add the memcache machine`ip in the settings.py in you project.

ok, point to /cache_admin/ ,you will see the key and length of each item,

that `s all: 
